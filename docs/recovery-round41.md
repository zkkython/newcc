# Recovery Round 41

Date: 2026-04-02

## Scope
Replace commit attribution hook placeholder with a safe executable baseline.

## Changes

- `src/utils/postCommitAttribution.ts`
  - Replaced placeholder hook script body with a functional `prepare-commit-msg` hook.
  - Hook behavior:
    - reads `CLAUDE_CODE_ATTRIBUTION_TRAILER`
    - appends trailer to commit message file when present
    - skips merge/squash autogenerated commit messages
    - ensures idempotency by checking for existing trailer text
  - Added hook marker (`claude-code-prepare-commit-msg-hook`) for idempotent re-installs.
  - Added non-destructive install guard:
    - if an existing `prepare-commit-msg` hook is present and not Claude-managed, installation is skipped (no clobber).

## Validation

- `node scripts/recovery/scan-missing-imports.mjs`
  - unresolved refs/edges/modules: `0`
- `rg -n "HOOK_MARKER|CLAUDE_CODE_ATTRIBUTION_TRAILER" src/utils/postCommitAttribution.ts`
  - marker and executable trailer path present
