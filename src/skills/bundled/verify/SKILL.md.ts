const content = [
  '# SKILL.md',
  '',
  'Section: Verify Skill',
  '',
  'Define a strict verification loop: reproduce, test, inspect, and summarize evidence.',
  'Require explicit PASS/FAIL/PARTIAL verdict with concrete commands and outputs.',
].join('\n')

export default content
